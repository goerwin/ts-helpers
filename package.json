{
  "name": "ego-node-helpers",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "version": "0.0.0-development",
  "description": "",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "test": "jest",
    "clean2xxxx": "rm -rf .nyc_output && rm -rf coverage && rm -rf dist",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "test2": "mocha -r source-map-support/register --compilers ts-node/register src/**/_tests/**/*.ts",
    "watch-coverage": "chokidar src --initial -c 'npm run coverage'",
    "coverage": "nyc --reporter=html --reporter=text npm run test",
    "watch-test": "chokidar src --initial -c 'npm run test'"
  },
  "author": "Erwin Gaitan O <erwingaitano@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "jest": "^26.6.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "chokidar-cli": "^2.1.0",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  },
  "files": [
    "dist",
    "LICENSE",
    "readme.md"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/erwingo/node-helpers.git"
  }
}
